import 'package:flutter/material.dart';

class RafflePage extends StatelessWidget {
  const RafflePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('No raffles yet'),
    );
  }
}
